Introduction to LLM Alignment and Social Behavior
Hello.Good morning.Vanish.Am I audible?Hi, Professor Plavan.Good morning.Yes, you are.Good morning.Thank you.Hopefully the voice is fine now.Someone wrote like the voice is low.OK, great.Thank you.And let me know whether the slides are visible.The title slide.Yes, Sir, we can see your screen.OK, Sure.Thank you.OK, good morning everyone.So today we are going to talk about the third part of post training of large language models, which is LLM alignment, right.So if you just try to recapitulate what we did in the last two weeks.So we are talking about models which can be post trained in a parameter efficient way.Right now that was a kind of idea that people implemented after we have a pretend model all be right.Now, as we have our model, we would be interested to query the model and we would expect certain responses from the model.And of course we want our responses to be correct with respect to the question or the query that has been posed to the model.But at the same time, we want the model to exhibit certain kind of social behaviour as human being generally does, right?So for example, the model should be helpful and the model should not spit out some toxic content, right?So these kind of things, as you can see, these are kind of social signal and then social signals are not really easy to capture by just labelling an output, whether that output is correct, incorrect, so and so forth.So we need to do certain amount of additional work to make this correct model to tune towards the social norms that we generally exhibit as human being in the society and there we will see to them.That kind of idea is what we call as LLM alignment.We are aligning the large language models to the societal behavioural patterns and today our focus and there are different methods that have been proposed in the literature.But today we will be having our focus on two different methods.1 is reinforcement learning from human feedback.In short RLHA and direct preference optimization.This reinforcement learning from human feedback was a kind of legacy strategy which powered this alignment of large language models like chat, DPT and others few couple of years back.And we will shortly see that there are certain issues with this kind of approach and that is where this direct preference optimization will come into play.
00:21:03 - 00:24:01
Learning Objectives and Triple H Framework
So today our focus would be to train the large language model to behave, behave according to the social norms.So let us look at the learning objectives for today.So of course the broad objective is how we can teach the LLM's to behave and behave in the sense of the societal behaviour and societal norms.And as you can see, this is a very abstract notion and how we can make it more concrete.So in that respect, we will talk about something called Triple H framework where first it stands for helpfulness, the second H stands for honesty, the third H stands for harmlessness.And we will see that this again is a kind of abstract framework.And we want to make that operationalize so that they can be translated into the language of large language models.And then we can train the models to follow this kind of behaviour, which is kind of specified by this Triple H framework.So in doing so, we would use something called reinforcement learning to align the model to the social norms.And then we will be talking about a method which we got direct a preference optimization.There we would be talking about a a reinforcement learning free method.So let us talk about this framework of this Triple H.So you want our model to be helpful.So whatever we have put a query, we should get a very the model should be putting forward a genuine effort to help the user to with a complete or correct answers, right.And if we talk about honesty, so there it should not fabricate things it it it should not fabricate things with confidence.So, whenever the model thinks that there is a limitation, it should say it upfront without cooking up a very fictitious thing.So that is what we call as a sycophancy.The model should not be a sycophan.The third one is harmlessness.Now, there might be situation where the correct answer is something that is harmful and the model should abstain from spitting out this harmful content.So let us take a very concrete examples for each of these.So let us say our user puts forward a query like my Python script throws a key error.Here is the code with the code the user has put forward a kind of error that the user has encountered while writing the code.
00:24:02 - 00:28:56
Triple H Framework: Helpfulness, Honesty, Harmlessness
Now a helpful way to do that is that it it tries to extract the exact line where the error has occurred and explain it to the user why that error has occurred and finally suggesting a a fix to correct that right but unhelpful method would be response would be something like this.Now it will say give the user a very general response like it will happen when the dictionary key does not really exist.And you should check your code carefully without helping the user with the particular line of code where the error has really happened right.So though this answer is really correct but it does not help the user to solve the problem.Now let us look at that honesty dimension.So let us say the user has put forward a query.Did Einstein fail math in school?And there is a kind of popular myth that Einstein though with a very high IQ has failed in in the math exam, right?But that is not really true.The honest answer would be no.That is a popular myth.Einstein really did well in all math exams in young age.And this confusion happened due to some other reason like the grading, scale conversion and other things.So the model is being honest that it has looked into data.It is not going with the popular belief to answer the thing.But a dishonest answer would be like it will be going with the popular myth and it will start saying, yes, you are absolutely right that there is a kind of of evidence that Einstein failed in mathematics, right?And it will be fabricating some evidences also right now in the domain of harmlessness.So user asks for like give me a step by step instruction to synthesize a bomb, which is a very harmful question to ask because someone with that knowledge can do harm to the society.Right now, a kind of correct model or a line model would decline this request explaining the user that why this request cannot be catered to the user, right?Or a harmful message would be like it will be complying to the the request of the user by by telling how to create a home when the user by or tries the some trick like I will be using this knowledge in my chemistry lab, right?So in that case, the user might come up with an answer which is full, right?So what we want, we want our model to be helpful, honest and harness.So let us talk about the trade off between these, right?So if you want to increase helpfulness, that may hamper the other dimensions.So there is a kind of tension between these 3 dimensions.So let us talk about a scenario whether the user asks for detailed medical dose, right?For, for some disease.Right now the helpful answer would be giving a direct answer to what kind of dose it the user should use exactly with all the details.But it might hamper or it will be in class with the harmlessness because there might be a risk of misuse or if the advice is wrong then it may Causeway harm to the user.So this helpful less dimension and the harmless dimension, they are in kind of tension or contention, right?The second scenario, let us say the user has written a draft which has got a flawed argument right now, even if there is a flawed argument to make the user happy, the model can say it is a very nicely written essay without pointing out the flaws that the user in the user's essay.But an honest model will always point out to the flaw.And you see this helpfulness is a kind of having competition with honesty.The third one, like there might be a borderline Security question like how does SQL injection work?Now this if you want to.So this is a kind of a security related question.And if your model want to be harmless, then it it, it sees that there is a kind of a possible way of attacking a system and it abstains itself from giving the answer, right.But it might be helpful for the student who is trying to learn this particular subject, right.So even there is a kind of contention.
00:28:56 - 00:33:06
Preference Alignment and Ranking Importance
So if you want to make your model helpful, only helpful, then it might comply with harmful request, It might spit out harmful request.If your model is harmless, then it may refuse everything.Even if the answer is not that risky, right?May be mildly risky, if the model is honest, then it can spit out harmful response as well.So we can see that there is a kind of tension, right?So now again, these are very abstract ideas now.Now how to translate that in the language of say machine learning or deep learning or in the sense of as we train the large language model, so how to translate this abstract idea into that.So in that respect we like to talk about few things.So first thing that is important that this Triple H is a specification, right?And we want to translate that into an idea of preference alignment.What do we mean by that?Now, if we think about a query and with respect to that query, there might be multiple answers.And out of those answers, the user in general may prefer one answer over the other answer.Right?Now we can think about 2 answers and we are trying to judge the helpfulness between these two or compare the helpfulness between these two.We can prefer one more than other.Now we can use that our training data to train a model, right?So now, now though this HHH is an abstract idea.Now gradually we are moving towards making this more concrete in terms of training a large language model, right?So from specification to preference line.The second idea is that ranking is important but not rubric.So this idea will be expanding more in the coming slides.So as I talked about this preference data you have to compare between two responses or among multiple responses.I you can rank them but it is not really important that you specifically say that.The first response is having a score say 100 out of 100.The second response is having a score of 90 out of 100, which is very difficult to do.But the human being can easily rank 10 items very very easily.But they they it is very hard to find exact score for each of these items.Now if we have that kind of idea, then the model that we will try to develop, it will try to model these kind of relative preferences between the responses and that will be act as a proxy to this specification like H8H.So we will be trying to build a queriable, queriable or differentiable model.And there we will be asking like how well does this response satisfy H8H in the terms of the preference data.Now we talked about the tension between 3 dimensions, right?And again, that is a kind of abstract idea.Now we want to make that operational, right?That should not be restricted into the theoretical domain.And this operationalization can be done via this modeling or learnable models, which we, we are going to talk about now.Now as we are seeing that for the last four items, we are talking about preference data that becomes our bread and butter at this point of time.We have to make this preference data as as similar to as a societal norms, as similar to as human behaviour.
00:33:06 - 00:39:16
Reinforcement Learning from Human Feedback (RLHF) Overview
And that is sometimes is very difficult to get right.And we will talk about how to really prepare that kind of data.And finally as we have the preference data and the baseline model which has already been pretend which we want to align with the stated preferences, then we will see there are different methods to solve that problem.More or less this is the core idea of translating this abstract specification OF8H2 triple H2 preference alignment model.So now we talked about pre training, we talked about fine tuning and now we are talking about alignment which in technical term we call preference tuning.We are tuning our model based on the preference data that we are collecting from the real users.So the first part of the discussion would be reinforcement learning from human feedback RLH.So before going forward to the next few slides, so there is a kind of disclaimer to start with.So this is a very challenging topic, whatever we will be discussing today.And of course they will be very much mathematically involved.And though it is challenging, at the same time these are very interesting.So I request your absolute attention of whatever we are going to talk about from now.So hopefully I have tried to make the things simplified.I have not gone for detailed derivations, derivations which will consume huge amount of time and confusion.So we will be talking about the tracks of the idea of RLHF and then DTO.So let us try to see what a language model does right.Very simple thing.So what we have, we have an input prompt, we have an input prompt.So this is my prompt and we are trying to see which is the next token and what are their probability.We are basically computing the probability of the next probable tokens, right?And in doing so we will be generating A probability distribution over the vocabulary, right?And we can see that the the token waste has got the the the best probability value, right may be if we are following a kind of of principle where we will be picking up the token which has received the maximum probability, we will be filling that out here, right.So now the next token that we are generating is that Kharagpur is a town in West, right.So our input from to the model is where is Kharagpur right and LLM in the first instance.So you are doing a kind of decoding here, right.It is generating Kharagpur right?Now in the second instance, this Kharagpur will be appended with the prompt which has been input to the model earlier, right?And then it will be taking this as an inter prompt and generating the next token as is with a form of probability distribution.So be be mindful that here we are also computing like what is the probability of is given where at if every step we are computing something like that.And then after we have generated is we append that to to our input prompt and then we feed that to the LLM in the next instant.And we are generating in.Now we are adding that to the input prompt, We are generating waste and we are adding waste to the input input prompt and we are generating Bengal, right?And that is how a large language model generates tokens at every time point, right?I hope we understand this from the earlier classes.Right?Now the question is we have preference data and we need to a kind of align our model with respect to that preference data.So that is the idea.Now what we generally do, if we have a data set and a model, we do some kind of supervised learning.So there we have the data X and the level corresponding to that and we train our model with this signal.Right now in reinforcement learning, the idea is that this part is absent, This part is absent.But what is present that whenever a model is generating something, model is receiving some reward.So, let us say if the model is generating waste, then the model receives a reward say 9 out of 10, right?Now if the model is generating M, the model is getting a reward of say 1, which is far less than what you receive for generating waste, right?So then you know the model, the model knows that I am getting positive reward, then perhaps I am doing good, right?So I will be maintaining that strategy, right?So that is the basic idea behind reinforcement learning, right?So let us see why we are going for reinforcement learning instead of doing a supervised way of aligning the model.Right now, what we have these preferences, as I talked about is the very, very abstract idea, the social norm, whether something is helpful, whether something is harmless or harmful, that entirely, that is very much subjective in nature, right?
00:39:16 - 00:45:30
Reinforcement Learning Setup and Agent-Environment Interaction
And that is why labeling that is very difficult, right?But if you generate a text and ask a human being that tell me whether this is helpful, perhaps the human being can tell this is helpful, right?So as this is very abstracting idea, labeling those would be very difficult.But here the there can be reward like if you generate good set of tokens then you get a positive reward.If you generating bad set of tokens, you get a negative reward, right?And that can help the model to learn better.So as we know as I I was talking about South model output something and human is really good at evaluating or selecting a good model output or demarcating that from a bad model output, right.So while the human being is doing that.He or she is looking at different other things like the social or cultural context, the ethical implication of what you have generated, or whether the response is really going with the current context.So all these kind of factors are taken into account for the decision that the human being is taking, right?So that is very difficult to capture with levels, right.But user can say that of human being can say whether this is a good or bad and based on that perhaps you can train your model, right.So basically we are going away from the traditional way of labeling the data and training the model in a supervised way.But while we are generating the text 1 by 1 at every time point, time point, we are getting a reward from the system in somewhere other or from the human being somewhere other.And we are taking that signal to tune our model more or less that is going to be our ID, right?So in a more diagram, diagrammatic term, what it is coming out to be.So we have one LLM agent and you can see that we have a decoder architecture here and we have an input prompt.And with respect to the input prompt, the decoder is outputting a response and the human annotator is saying that this is good or bad, right?On all the other things like the social norms, the behaviour, the ethical concerns, they are all hidden inside the human mind, right?So that is not really explicit, right?And that feedback is going to the model right now, as this feedback is not really a kind of a thing on which you can run a gradient descent or you can differentiate this.That is why this part of the feedback loop is cut off, right?You cannot use it directly, but we will see how we can use it indirectly.Now with this, let me take some time to describe the reinforcement learning setup.Then let us take a very toy situation.So you have a grit and you have a mouse.The mouse can take actions like going to adjacent cells, up, down, left, right, and in some cells like we have a doll cat, which is not really harmful, but the mouse can be fearful about it.So whenever the mouse is in that cell, then the mouse will be kind of scared and that will translate to a negative reward, though not with a very high negative reward.Now if the mouse is in this cell, then perhaps the mouse can grab the reward which is very high.Right now as the mouse is in this cell, it is a very bad one.The mouse may get trapped and there you get a very negative reward.Right now here the mouse has to learn how to move in this grid and what is the signal or objective that the mouse will be having that it will be trying to maximize its reward without getting trapped.Whenever this mouse is getting trapped, then the game ends.So what we have, we have an agent who is working in this situation and the agent here is the mouse and we have a state.State is the current configuration of the world.What is the current configuration of the world?So the grid as we can see and the placement of the elements in the grid.So that is our position.Specifically the position of the mouse is the state description for us at this point of time.Now at this state, the mouse can take many actions.It can go to different cells.So we can define the actions like it can go to the right, go to the left, go to the, go to down.It is so and so forth.Right Now as the agent is moving from 1 cell to the other cell, so that means the coordinate of the agent is getting changed.Now as the coordinate of the agent is getting changed, so that means there is a change in state, right?So you are transiting from one state to other state as the agent is taking an action, right?And we need to talk about reward.So that is where the that is what the agent thrives on, right?So what are the rewards?So if you are moving to an empty cell, there is no reward.So if the agent is moving to the doll cat cell, then a reward of -1 or very small negative reward.Now if the agent is moving towards a trap, I should have given a larger reward may be -10 or whatever it is, right?
00:45:30 - 00:52:09
Trajectory and Policy in Reinforcement Learning
And of course, whenever the mouse is getting trapped, then the the episode ends, right?And you can start from any other position.Now if you the agent is moving to the towards the cheese cell, then it is it is getting a reward like 100, right?So now the objective of the agent would be to learn how to act.So basically the object objective of the agent would be to learn how to act at a given state.So I am in this state, mouse is in this state.Now from this, what would be the most beneficial?What is the action that you should take?You should avoid this action down.But you can take an action right and then up.But if you take an action left, it is not going to harm you, but it may take you to longer time, a longer time to reach to to the maximum reward, right?So may be this is the one you should learn, right?So that is why our model or policy or at at a given state, what action that I will do is what we call as a policy, right?So the policy is again I am iterating this so you are in a state you can take multiple actions and you need to think or check what action you can take at that point of time.Right now if you are thinking about a probabilistic world, so may be if you are in this cell, so in this particular cell, then may be if you are moving to this, you can move with probability say point 6 to this say 0.1 to this say 0.1 and say to this say 0.2.So you have a kind of probability distribution over the actions that you can take.So this is a probability distribution.So that means for all the available action for that particular state, you have a probability value.So I have 4 actions in this state.And I know that this is most probable.This is a bit less probable, this is bit less probable.And this is having a better probability, right?So this is very important for us.And we are kind of picking up 1 action out of this probability distribution.And if we follow, we are following like I want to follow the action which is most probable, then we will be following this action perhaps, right?So this is what we call as picking up an action from the probability distribution which defines our policy.Now while the agent is taking an action, let us say agent has selected an action and executed that action, right.So after the execution of the action that there is a change in the state of the model.And in this case what is the change if the.So let us take an example.So let us say the agent decides at this point of time I will be taking a downward move, right?So that means the agent is now in this location southern as the location is getting changed.So there is a change in state, right?And what has triggered that transition?The action that I am taking a downward movement, right?So that is why we have a kind of condition that I am in state S TI have taken an action AT and what is the probability that I will be in this state, right?Even if I am taking a downward action, there might be uncertainty that I will be landing up to here, right?There might be a probability that even if I am intending to go to downward may be with some fault or other, I stay back here.So there might be a kind of uncertainty.So that means again that will be translated to a probability distribution.So taking an action, what is the probability that I will be transiting to a new state, right?So that is what we call as the transition model right now.Finally, our goal would be to maximize the expected return, right?So what is our return?Our return is what is the reward.Finally, I am getting if I am following certain points.So the setup is like this.We have an agent, an environment.Environment tells you what state you are in, and in that state the agent is taking an action and what is facilitating that?The pile, what is the action given St.?And after you have taken an action, you are transiting to a new state S t + 1, right?And as you are transiting to that state, you are getting a reward, right?So if you are moving to an empty cell, you are getting a reward 0.If you are moving the cell to the cat, you are getting a reward -1 if you are moving to a trap, you are getting a reward of say -10 or something like that, right?And if we are following that multiple times, that creates A trajectory.Your model starts with a state S 0 and you have taken an action A0 with reward R0, right?And from that you have transmitted to South one, right?And in South one you are taking an action A1 and you are getting a reward R1.And you keep on doing that, right?So if you go back and try to simulate a trajectory in this example, so this is my S 0.
00:52:12 - 00:59:01
LLM as Agent in Reinforcement Learning Framework
I am taking this action.I am coming to South one with reward what with reward 0 right from this I am transiting to this S 2.The reward I am getting -1 right from this I am transiting to this S 3.What reward I am getting 0 from this I am transiting to this state S 4.What reward I am getting?I am getting 100 right?So that is what we call a trajectory.So if you roll out this policy, we get a trajectory.So this is what the schematic diagram of reinforcement learning.We have the agent which is governed by a policy.What does the policy tell?The policy tell?What is the action that I can take given that I am in state St.OK and I select an action and as I have executed that action, there is a change in the environment, right?And the change in the environment is like you are getting from South how to get to South t + 1 from South T So that is what is the definition of this transition function.Right now you are getting to a new state and the agent is known to the new state and at that state you are getting a reward of RT.So this is the overall Theta.And again if you roll this out, you will be getting a trajectory.So this is the trajectory.So these are the components in T0 S 0, S 0R0.These are the components at TT equal to 1.And this is the component in the final step.This will be capital T, capital T Now depending on your problem, this can be of finite horizon or infinite horizon.Finite horizon means that you have a kind of restricted time limit time step where your game will end where your trajectory will end, right?So you have a restricted T event.But whenever T is equal to infinite, you can keep on playing the game forever, right?So that is what we call as infinite horizon.Now, let us transfer this idea to language model, right?So in order to model LLM's in the framework of RL, we need to decide what is going to be my state, what is going to be my agent, what is going to be my action.So let us think about this.So the agent is the language model itself because it is taking the decision what token I will be generating now, right?Or basically how do we act?The state is the current prompt, so that is the state I am in, right?The action action is given that current prompt, what is the token I am generating?So that is going to be my action, right?So the token that I can generate given that I, I am given with a prompt, right?So that is going to be an action.And the reward model is what you have generated some action or sequence of action and check whether this action is good or bad, whether the response you have generated is good or bad, right.So that is what is going to be my reward model and what is the policy?Policy is basically what is the probability of a token.So for example, waste given the input prompt, what is the probability of Kerala given input from, what is the probability of Australia given the input from, right.And that is how we can translate the RL setup to our language model, right?So this is going to be action, this is going to be my state and this is going to be my policy, right?So given a state the probability distribution over my action.So these are my possible actions and I define a probability distribution over this.So now we need to think about what is going to be my reward model which we do not know at this point of time.So we will reserve this question for future and we will discuss that further.So now let us kind of translate the diagram that you have discussed in the terms of agent and environment to the LLM language, right?So our agent is the LLM which we have already decided, right?And environment is a kind of transition function.Transformation function.What is my transformation function?Very simple.I will be adding the token to the existing prompt.So I was an initial prompt.I am adding the new token which you have generated.So that simple rule governs my transition function, right?So my state is the prompt.So just remember this was my St.right?And what it spits out an action, right?And what is the action?Action is the new token.And what is the kind of embeddedment function or the transition function?We will be adding the prompt with the new token that have generated so that that is what we have written, right?And that transit that that makes a transition from South T to S t + 1, right?So what is South t + 1 St.plus is 1 is the new prompt adding the new token to the existing prompt, right?And again, I have not talked about reward.We will talk about that.And my policy is now going to be given a prompt, tell me which token I should generate exactly.That is what you want to learn.Think about LLM, what it does given a prompt, what is the next token that we are going to generate, right?But in the RL setup, that is the action that I am going to take.So in summary, we have the policy and policy is basically the LLM, which is kind of modelling that what is going to be my next token given the prompt, right?Initial state is the first prompt that you have input to your model, right?
00:59:02 - 01:05:12
Expected Cumulative Reward and Policy Optimization
Action Action is going to be the next token that we are going to generate, right?The state, the new state is the token generated plus the prompt, right?And the reward.Reward is something that someone should say whatever you have generated that is a good response or a bad response, right?So that's why sometimes you can see that the chat, other things, they will ask you that I have given 2 answers, tell me which one is better, right?So that's where you are giving the signal that out of these two, which one is better, which one is worse?Right?Now the look at the production system of large language model in the decoder only architecture.So this is my initial prompt, OK.And in the first time step it generates SAT right?And I append that right and I feed that to the decoder its IT generates the new token, I feed that back, it generates another token, end of sentence.And this is my final output.So that is more or less the mechanism of how the tokens are generated in larger language model right now.Let us try to translate that into this RL workflow.Initial state prompt policy.So given this, I am generating down.That is going to be my policy, basically what I can generate.Now the policy says that you generate down South.That is the action that I have selected.And what does that action do?That happens down with the existing prompt.So this is my transition function, right?And that takes me to the next state.What is the next state?The next state is the other prompt, the prompt plus the new token generated.I feed that back to the policy, generate the next token, and all these would be used to generate my trajectory.So basically I will be taking the current state, current action and the reward and every step I know what is the reward whether that is bad or good or bad and we generate A trajectory.We are just rolling out the mechanism and generating a trajectory.Now what does an RL learn?Very simple answer.It learns a policy.And that exactly is the goal of LLM, right?Given an input prompt, tell me which token I should generate, OK?So you can see that there is a kind of convergence within the objective of this reinforcement learning and objective of training a large language model, right?And basically in the case of LLM, I am trying to learn a policy that what is the token I should generate given a prompt.Now let us talk about trajectory.OK, so this is my trajectory after rolling out this RL setup for T time steps.Now I need to if I want to compute the probability of this trajectory.How to really do that?Because the things are probabilistic nature, the action production is probabilistic, the state transition is probabilistic and for that we need to understand the production system.What is the production system?I start with the start state right, and I have my Ï which tells me that what should be my next action, say a zero given S 0 right?This is again a probability probability distribution.And if I take this action, what is my next state?So your state transition function will tell that what is the probability of say South one given that you were in state S 0 and taken action a 0.So the probability of the trajectory will be a kind of multiplication of the starting probability of this.So there is always a kind of starting probability of any state and this one and this one, right?So basically the component that we are trying to measure is the probability of a trajectory given a policy.So here we know that there is a policy.If I roll out the policy, I can generate A trajectory, right?So think about the mouse grid example.I am in a state, I follow a strategy that I will be taking the say I will be generating the next actions the the places that I can move to, right?So that is a kind of probability distribution, right?So from this state, from this state I can go to this, I can go to this, right?So if I choose this and this choose choose this, then choose this, then choose this.This is a trajectory, right?Now if I choose this, then this one, then this one, I have another trajectory, right?And I can compute the probability of this.I can compute the probability of this, right?So, this probability is the probability of the starting state and probability of the action in a given state and probability of the next state given that I was in a state I have taken a particular action, right?
01:05:13 - 01:12:06
Gradient Descent and Reinforcement Learning Challenges
So let us try to unfold it.So probability S 0 multiply probability A0 given S 0 right, I am picking up action a zero.What is the probability of picking up action a 0 right now?As I have picked up a zero then what is the probability of South one given say South 0A0?Right now I am in what state?I am in state S one right?And from that state I need to check which action I can pick up right now If I pick up action A1 then what is the probability of S 2 given say S 1 and a one?And we can keep on doing that and a more succinct way to write this down South this, right?So in essence, we can compute the probability of a trajectory, which is going to be useful in our later computation.So now how to make use of this, right?Of course, I have the objective that the next token that we will generate, it should always guarantee the maximization of the reboot, right?With respect to that, in a language model, what is the notion of the trajectory, right?So it is nothing but the series of prompt followed by the next token that I have generated, right?So I started with this prompt, the next token that I have generated is Kolkata, right?And by the rule, I know that I will be adding that or appending that to the prompt where is Kolkata to get to the next state, right?So you started with the initial state where is Kolkata and the action that we took is a 0, right?And it is generating is right now.So in the next state where I am putting this where is Kolkata and Kolkata to be my next prompt I am generating is that is my action at A1 right now.I generated in right now, what is my which state it has?It has been generated from?It has generated from South 2 where S 2 is a combination of where is Kolkata, Kolkata and is and being in that state, it has generated A2 and so and so.So now we can see that this trajectory is nothing but a sequence of prompt next token, adding that next token with the prompt, the new prompt next token and a sequence of that.Now we have talked about trajectory.Let us talk about what do we mean by the objective of the RL in a very good term term, we know the objective of RL is to maximize reward, right?So we need to think about what is a kind of a reward a trajectory can bring to you, right?That is very simple.So at every step of the trajectory, you know what is the reward that you are getting and you can keep adding those reward to get the reward of the trajectory.And that is what we call as the cumulative reward.So basically there are two ways to think about it.If your horizon is finite, as we have talked about earlier, we can start, we can just sum up the reward value at each time point.So it is very simple right?Now if it is infinite horizon, then if you do not, you just use this formula and replacing T with Infinity, then your reward is going to be infinite, right?And that should not happen, right.So to to to counter that we use something called discounting factor.So as we are moving towards future then that future will bring me very less amount of reward.We are discounting that with very high factor and we are keeping this gamma to be less than one, right.But here we will be considering this finite horizon because we will be stopping our generation at a fixed time point or some time point.Right now, after we have computed the the reward for a trajectory, we should think about the expected cumulative reward.So what do we mean by that?So basically this is the property of a policy.A policy as this is the probabilistic in nature, it can generate many trajectories as we have seen in the grid world.If I follow a certain path, it will generate A trajectory.If I follow a different path, it will follow a trajectory.And we have computed what do you mean by a probability of a trajectory, right?And now we have computed what individual trajectory, what reward it can bring in, right?And we can use these two to computed the expectation or expected return of the reward, right?So, and we know the formula of expectation.So if you have a random variable X, right, which is governed by the probability distribution, say P so it will be like say, say IPIXI.So this is the formula for expectation.So think about a rolling of a die, right?So you have the values like 123456.And if we have a fair dice, each of these is having a kind of probability 1 by 6, right?So the expected value of the die roll would be 1 + 2 + 3 + 4 + 5 + 6 by 6.So this is the expected value of a roll of a die.So we are doing exactly the same here, right?So the probability of a trajectory and the return that that trajectory receives, right?And we sum that for for all the trajectories, right?So basically we can judge a policy by looking at what is the expected cumulative revert, right?And of course, I will be selecting a policy which has got maximum expected cumulative revert and that is going to be my objective to learn, right?
01:12:08 - 01:19:00
Reinforce Algorithm and Variance Reduction Techniques
So basically I want to come up with this.We have already computed, right?This is nothing but the expected cumulative revert.If we have the policy Pi Theta and what is Ï Theta?Pi Theta is a kind of policy.May be we are learning that policy with a neural network parameterized by Theta, right?And we will be learning the parameters like Theta parameters, right?And we will be maximizing based on these objective value which is the expected cumulative reward, right?And we will be picking up the parameter configuration which maximizes this quantity, right?And we can do a simple gradient descent or gradient ascent.We did that, right?We have a loss function and you want to maximize that, right?And we have done gradient descent, right?But as it is a kind of maximization problem, instead of doing gradient descent, we will be doing gradient descent.And in order to do that we need to compute the gradient of the loss and we know we are doing in the kind of iterative fashion.So I am not going to expand this further because this has been covered perhaps earlier and this is very basic of machine learning.Now this quantity is very important the gradient of the loss function which I am not going to derive, but it looks like this.So again the the expectation would be over all the trajectories that the policy can generate and it will be a kind of grad of this quantity.So I am not going into further detail because this derivation is very involved at this point of time.If we can understand this formula that is fine.I mean these are all while you are implementing this, you will be computing this kind of stuff, right.But at this point of time understanding this formula is fine.Now we would be a kind of now what we have, we have a policy network, this Pi Theta, which is represented by a neural network, right?What what it, what is the input to that input to that is a state, right?What is the output?Output is a probability distribution of what is the probability of an action given the state, right?And I know that for a policy, what is the expected reword, right?And I need to maximize that, right?So we have a policy network Theta Ï Theta which is has the parameter like Theta and we want to maximize the expected return, right, which we have already discussed earlier, right?And that we can do by performing gradient stochastic gradient ascent.And for that we need to compute the gradient of this loss function, right?But the problem with this approach is that it is computationally intractable.Why?Because this expectation requires that you roll out this policy to generate all possible trajectories.So think about this grid world.We are starting from here and you will be generating many trajectories.You can start from here.You will be generating many trajectories.So the number of trajectories are very huge, right?So in order to compute this, you need to enumerate all these trajectories and then only you can compute this.So that is what the problem is, right?So let us see how to tackle that, right?So this was the original problem to be solved, right?Where I will be picking up all trajectories to compute this expectation, right, which is untractable, intractable.In order to tackle that, what we will be doing, we would be sampling some of the trajectories and how we can do that?I can start from any state in the grid and let us say I will be rolling out once, rolling out twice, I will be rolling out thrice, 4 times.And you keep on doing that and collect 100 such samples, right?There might be thousands, there might be lakhs of samples.But out of that you are picking up say may be how 1000 or 10,000, right?And that is going to be your training data, right.So that is your training projection.Now you can simply replace this expectation computation to be computed over only this D samples out of many samples which you have rejected, right?And the inner part remains to be the same.Here.The crux is we are not using all the trajectories that can be generated by the policy.We are just rolling out say 1000 times, collecting 1000 trajectories and we are estimating this gradient, right?So that is the simplification and that makes this computation tractable.And with this, the kind of algorithm that we get is the reinforced algorithm, right?So again, the steps are more or less same as we have talked about earlier in the normal policy gradient algorithm.So it create a neural network that represents our policy, right?And input is the current state, output is the probability distribution of all the actions.Right now, we will be using this network to sample some trajectories, right?And using those trajectories, we will be computing these gradients.We will be estimating.So this is not the true gradient, but it is an estimation of the true gradient, right?And then we will be running a stochastic gradient ascent, right?And we will be going back to the loop 2.And we will do till the convergence or number of epochs.
01:19:02 - 01:25:07
Advantage Function and Policy Network Training
Now again it is easier but it is not really efficient.So why is that so?Every time while you are trying to tune a single parameter, you are doing this activity right?So you are sampling this set of trajectories and this approximation is proven to be unbiased.What do you mean by that?This estimated gradient G hat if we do so, it is very much closer to the true gradient.How do you compute the true gradient?By enumerating all possible trajectories.So if you do this kind of approximation so they are more or less, more or less converged.So that way it is called unbiased, right?But the problem is it has high variance.So what do we mean by that?So let us say in one time you sample some trajectories in one role, in another role you sample other trajectories, in other time you sample other trajectories.So the gradient that we will be estimating here, gradient that we will be estimating here, gradient that we are going to be estimating here, they are going to vary a lot.And that is really a problem.So the problem is your training will not be stable as your gradient is because at every time you are sampling right, you are sampling this V number of of trajectories.So that is what the problem is.Now, how to reduce this variance?So let us take a deeper look into that.So think about a model.Let us go back to basics of machine learning.So you have a model like say W 5 X to the power 5, W 4X to the power 4 plus W 3X to the power 3 so and so forth and say W 0 equal to say C So this is 1 model.Other model is say W 1X plus W 0 equal to C, right?So this model is very complex, much more complex than this, right?But we know that this model has very high variance as compared to this model, right?Now how we can reduce the variance?Simply we can drop some higher order terms, right?So that can reduce the variance, right?Now if we look at the computation of this gradient, look at this term, right?This is what this is the accumulation of the reward for multiple time points.If you have T time points, you are adding T terms here.Now one thing think about it.Whenever you are taking an action at T equal to say five time point, that action may have an influence to the action that will be taking at T equal to 6.Action that you are taking at T equal to 10, action that you are taking at T equal to 100 but not T equal to 1 because your current action will not affect your past right.So while you are computing these quantity at T equal to say 6, you should not consider the terms R1R2R3R4R5.I can consider R7R7R8R9 so and so forth, right?Because your action would be affecting the future actions but not the past action.And that is one way to reduce terms in your formula, right?So basically we will be revising this, we will be replacing this with this quantity which we call as the reward to go.So what is the reward that I am, I will be collecting in future not the reward which I have already collected, right.So that is what the reduction is, right.So if you are computing at T equal to say T prime, so you will be interested in all the reward from T prime to T not T prime to say T equal to 1.So that is one way to reduce the number of of terms, right?And of course as you are reducing the number of terms, it is reducing the variance, right.So that is the kind of idea.So removing terms in the experience this expression that reduces the variance.So other way to reduce the variance is that you have a baseline term which you can deduct or subtract from the original term.So this is another known strategy.So this is what we call as the baseline.Now, what can be a baseline?So the baseline is dependent on the current state, right?You can have a constant baseline, but that may be that may not be good, right?And you want this baseline to be dependent on the current state.And if this represents the utility of the current state, that will be even better, right?
01:25:08 - 01:32:23
Proximal Policy Optimization (PPO) and Reward Hacking Prevention
And that is where we are coming up with the idea of the value function which tells how good is the current state, how promising is the current state.So that can be a good baseline, right?So how to compute that?Very simple.So you so earlier your trajectory you are starting from South 0 right now instead of starting from South zero, we will be starting from that state only.So you are computing the value for this state, right?So what I will be doing, I will be running some trajectories.I will be rolling out the policy, getting some trajectories right and computing the reward for each of them, right.And we will be getting the average of those rewards, right?And that brings us to the value.I understand these ideas are really difficult, but it cannot be held because these are these forms, the core of this training, this model.So again, I will be taking the doubts one by one.It may take some time, but please be with me at this point of time.I understand the ideas are very, really complex, right?So here one thing we should understand that the the quality of the state is very important, the current state, right?And that acts as the baseline, the quality of the state.So now we have this revised formula.So with this, we will be taking a pause at this point of time and we will be continuing this after taking some of the questions from you.And yes, so I think we can go for mentee at this point of time, a quick mentee.OK, the first question is on the screen, what does the H in Triple H stands for?OK.So is it visible now?Yes, Sir.So what does the H in Triple H stand for, aside from helpful and homeless?So the options are human, honest, holistic and hierarchical.So let us move on to the correct answer.So the correct answer is honest, very straight forward.So the next question in the RL formulation of LLM alignment, what does the action correspond to?The first option is the inter generated response, the reward score, the next token to generate the prompt given by the user.The correct answer is the next token to generate, and most of you have got it correctly.The third one is what is the primary goal of a reinforcement learning agent's objective function.The first option is to minimize the number of action taken, to maximize the expected cumulative reward overtime, to memorize the exact sequence of states visited, to minimize the size of the state space.The correct answer is to maximize the expected cumulative reward overtime.And most of you have got it correct.So I understand that the things are are difficult.But with the responses that I have seen, perhaps you are getting the ideas again.It is not like that you can get the ideas at a single go.I mean you have to go back and study it again.This is really important and this is something that is powering all the large language models today because this large language models are getting more human, more social.And this is the way.And I really cannot skip this rigor that I am trying to present.But I think in the summary part, I will try to make it more concrete and more practice oriented.That will help the thing for you.And it's OK not to understand everything, every formula at this point of time.But while I will be talking about the architecture, perhaps you will be getting a sense of what is happening.OK, so with this, let us get to the questions.Should I answer?Where's the screen?
01:32:34 - 01:37:31
PPO Training Loop and Model Architecture
OK, yeah, so let's get to the questions.OK, question by Mr.Pandey.Shouldn't the state be prompt plus the LLM we are using and the number of parameters of the LLM, QKB, cache, etcetera instead of only prompt?So let us try to look at in this way.So LLM is a kind of module with all the QV, KQB cache, the number of parameters, everything, right?And this model is doing what is taking an input and generating a new token, right?And what we want as output after alignment, we want a revised LLM which does similar thing but in a better way, right?With the preference aligned way, right?So in this case, what is the state?The state is something the agent sees.And if we agree that the agent is the LLM, the LLM sees only the input and input is what input is the prompt, right?And what action it's taking, it's taking only the what is it is generating what it can generate at that point of time.So in that way, the prompt is only the state and the generated token is the action.Question or may be comment by Mr.Anantri didn't understand the future cannot be changed the past topic as explained by top.Please explain once.So here the idea is that so let us say you are taking a series of actions, right?And at each action point whatever decision you are taking, there might be a repercussion of that in the future actions, right?But whatever decision, whatever action you are taking, say at T equal to 10th point of time, that kind cannot affect the action that you are taking at T equal to 1.So whatever reward you have got there, that is not going to change.But whatever reward you are taking at T equal to 10 that might affect or whatever action that you are taking at T equal to 10, that might affect the action or the reward that will be getting at say T equal to 100.So that is what I wanted to mean.OK, so I think we are done with the first session.Let us take a quick break.Again a difficult one, challenging, but be stay, please stay with me.We have something good in in coming slides.Two BHK flats for sale in Kanpur.
01:45:46 - 01:45:49
InstructGPT Workflow and ChatGPT Alignment
OK, so welcome back.
01:45:50 - 02:00:15
Direct Preference Optimization (DPO) Introduction
So I hope my voice is audible and The thing is also visible.Sir, it's a bit low.Can you please check once?Is it better now?Yes, Sir.OK, OK, great.So where did you stop?So basically we have the gradient to train our policy model and what is our policy model Basically Pi, Theta, ATST.This is what we want to train finally, right?And this is the kind of LLM model which we are going to train this being the prompt and this being the next token that you have to generate, right.And we doing doing so, we saw that there was a kind of problem of variance etcetera.And we used a baseline in the form of value function.Value function at this point of time, this understanding would be sufficient that this the value for this function is the kind of what is the maximum reward or expected reward not maximum reward.What is the expected reward?This state can give to the model right?So that is the now if it is very high then the value for that state will be very high, right?So what is the expected reward if we start from South S?That is why you see the starting point is South S 0 equal to X and that is where we are computing the expectation over all the trajectories that we can get from South right?Now there might be another way to look at the same thing.So what is the way?So let us say I was in state South.Now what I did, I took an action A and as as I do so, I will transit to another state right?And then while I am doing so I will be getting a reward R Now I can also assign a value to a state action pair which says that if I am at state South and have already decided I will be taking an action A.Now whatever I am landing up with I want to check how much reward that I can get from that state.So basically I was in state SI have taken an action I have transited to S 2 and I check that how much reward that I can get from this.Now you can do this do this for multiple actions like South A1, S, A2, all possible actions that are applicable in South and choose the one which is maximum right.So this particular thing is what we call as the state action function or Q function which again decides or computes being in a state South and applying an action A.What is the expected reword that I am going to get?So try to contrast this with V Ï S What was V Ï S irrespective of what action I am taking, what is the kind of expected reward that I am going to get from South if I roll out policy Ï Now here I am asking a different question.I am state in state SI have taken an action a.And if I follow the policy Pi, what is the kind of utility that I gain reward that that I can get.So why I am and we can mathematically formalize that.So I know if I take an action at action A at South, we get a reward this right?And I know I will be transiting to South prime a new state, right?And this part forget about the formulation, this part compute what is the kind of utility of South prime, right?What is the utility of South prime?So that will give me the utility of a state action pair.Now we will revise this formula.The revision is that we will be replacing this R Tau T with Q Ï STAT.Everything remains the same.Now look at this quantity.What does this quantity say?This says that what is the average expected reward being at South T?This quantity says.What does this quantity say?What is the quality of taking an action being at State St.right?Does it say or or this entire quantity?Think about it.I am telling you and try to judge it.So I am in State South.I am taking an action A.What is the reward I am getting on top of the average.The average is the V Ï St.This is the average utility that you can get from South irrespective of any action you take.But now you are saying that at South T you are getting AT.So that means the this says that this is the quality of the AT.So this quantity is positive then you know that taking that action will give you a utility or reward better than the average reward, right?So in a simpler term, I can say this is an advantage value of taking the action, right?So it is a kind of difference between the average value that that St.can give and the value that AT can give.That is the difference.So basically if you choose a particular action at state S, what is the kind of of gain that you have in the reward on top of the average reward?And that is what we call as the advantage function.So this advantage function is basically the difference between these two.And perhaps you understand the significance of the difference.So this is what we call as the advantage function.So what how we translate this advantage term in LM?So basically again, I want to generate a prompt at that token at that point of time in a kind of expectation that this will result in better than average reward.This will result in better than average reward, right?So that is how we will be selecting our tokens at every point of time.So this is what so this is basically we are we are kind of of computing the expectation and how do we compute the expectation?We sample some trajectories, right?And we are doing sum over all the trajectories, right?So you have sample north trajectories, right?And this is where we are computing the expectation.And this is the inner part which is we have already understood right now this has become our final gradient, right?So basically to visualize this, so let us say your LLM has received this prompt where is Shanghai?And there are two actions which are possible.One is Shanghai to be 1 action and say chocolate to be another action, right?So of course, this should give you better than average rework than this.So you always try to tune our model towards generating this Shanghai on top of this chocolate, right?So this is what we have.We have to work on and as we have told earlier, we can compute the gradient and we can use the gradient to perform the stochastic gradient ascent, right, to update the parameter as we did earlier.Now there is a problem here.The problem is this way of sampling and using those samples, right?So how we are doing that at every time step we are sampling north number of trajectories, right?And after you have ran one epoch, you, you forgot that you throw away that and sample new trajectories N more trajectories, right.So even if you are updating a single parameter, you are sampling N trajectories.That slows down the training process because at every point of time you are sampling new trajectories, right?So that slows down, right?So this is an inefficient method.We have to make it efficient, right?So the way we do this, we use an older policy network.So this policy network, how it is happening again, the policy network is just an LLM which you are fine tuning right now.You can think about an older copy of the LLM.You keep an older copy of the LLM from which you can sample some trajectories and use those trajectories to train the current version of the LLM to fine tune the current version of the LLM.So you have the current LLM, you have an old copy of that and an updatable copy of that.Right now you do not tamper this.What you do, you sample trajectories from this old and feed these trajectories to this to fine tune.Right Now what you can do, you can reuse this in multiple epochs while you are training this model, right.So that is the kind of approach that we will be taking.We are so here the keyword is that we are reusing the already sample trajectories for multiple epochs.So earlier what we are doing, we are sampling trajectories, doing an epoch, correlating those trajectories, sampling new trajectories, then again updating the parameters, throwing away the trajectories.But here you are not doing that.We are sampling the trajectories from the old model.Then we use that for multiple epochs.So for that we have to do certain adjustment.I am not going into the theory.The adjustment is that we will be using this quantity.This quantity is that the first one is the current policy, the upper one is the current policy.So Theta is the model parameter for the current policy and Theta old are the parameters for the old policy.And this old policies offline which we have already trained that is offline, right?And we compute the policy ratio, right.So it is like how much the probability has got better in the current policy than the older policy.So it it measures that amount, that quantity, right?And we fit that in the loss function.Now we have this advantage function as it was earlier there we are replacing the other part with this policy ratio.That is the only change.Other part of the loss function remains to be the same, right.And of course, we want to maximize this maximize with respect to the Theta and Theta is what Theta is the parameter for the online LLM.That means which is which is being updated at this point of time.So what is the idea?Now we are getting into the architectural space right here.The idea is that you make a copy of the current model, 1 being the older, other being the model which you are going to update right at this point of time.From the older model, you sample some trajectories and use those trajectories to update the current model in multiple epochs.You are using these trajectories in multiple epochs, right?And in order in order to make that happen, you are doing the adjustment to the the loss function by taking into account the ratio of the policies.2 policies, 1 the current policy, the other is the old policy.So that is the kind of update.
02:00:18 - 02:15:11
DPO Loss Function and Training Workflow
So now, so far so good.Now let us say you want your LLM to be tuned to be polite.So think about a customer support agent.Your customer support should always be polite, is not it?Now you feed this.Why is the sky blue now?You wanted to maximize the politeness and it generates something like bang on.This is a great question.Thank you so much for asking this.This demonstrates this is a demonstration of your shear intellect.Very polite, but will the customer be satisfied with this?No, because it is going away from the task it was entitled to do.Trusted to do.It is going away from the baseline criteria.What is the baseline criteria?Baseline criteria is the original model that you started with.Your original model was pretend with your customer support data which tells for a given customer support query what should be the answer.But in order to become politeness, you forgot that and what you do, you generally are saying something which is very polite without answering the correct one, right?So basically what you have, you have a reference model.What is a reference model?A model which was supervised, fine-tuned with the customer relationship data.Now what you did, you fine-tuned that to get a model which is Theta, which is polite Theta that, But that polite is is of no use.The problem is that the distribution with which this response is generated by the polite model has shifted a lot from the distribution that is generated by the reference model.That is what the problem is.You should not do that.In order to implement this Triple H framework, you should not go away from the correctness of your model.Your domain vocabulary should not go ahead.So that is what is something called reward hacking.So basically you are exploiting flaws of your large language model to elicit high reward response but unhelpful response.So that you should not do.So what we should do?We should penalize the model if it is deviating from the original model.So if you see your Theta polite is very very far apart from Theta Rep or Theta supervised, then you add a great penalty term.But how do you know that there is a deviation?How do you know there is a deviation?For that you need to know what this models output.This model outputs something like Ï ATST polite, right?This model outputs what Ï SFT ATST right?So this is the probability distribution.This is the probability distribution.We can measure the difference between these two by measuring the difference between these two probability distributions.So this should not be very different.So this notion of computing the distribution to distribution is captured by what we call the Kullback lever divergent or KL divergent.So again there is no need to go into detail.So whenever you will be implementing, you can use a library but just to show you visually.So this is a probability distribution, this is another probability distribution.And if you are measuring DKLPQ, then the difference is something like this this area, right?And if you are measuring DKLQP, so that will be kind of of with this, this area.So basically the area that is uncommon between these two and this measure is not symmetry.So this DKLPQ is not equal to DKLQ.So that is not something that we should we should bothered about.But the idea that would be important at this point of time that we are generating, we are having two probability distribution, one by the fine tune model or preference align model, other by the reference model and we are checking how much they are close, how much they are distant.If that distant, we will be adding a penalty term.Now your loss function is going to change a bit in order to add that penalty.So this was earlier with us.Now we are adding a penalty term which is the KL divergent between the current model probability distribution and the reference model distribution which is generally A supervised fine tune model.So this is what we call as the reward hacking prevention loss.So again do not be overhelmed by the formula.So this part we had already with us, right, This part we had already with us.On top of that, we are just adding a penalty term which is the difference between the models Theta and Theta rate.So that understanding will be fine at this point of time.So again, this part is there as it is, as it was earlier.This part is computing the difference between the current model and Oregon, a fine tune model and the supervised fine tune model which you started with, right.So this slide is going to be the master slide.This understanding this is, is really important and this tells you how this PPO works.OK, so now as we told that we need to have a old policy from which we will be generating the trajectories, right, which will be our training data right?Now, how do we generate trajectory for LLM?So we have an LLM, we can give a prompt and it can generate some output.Right now what you can do, you can change some hyper parameter of the model.For example, temperature is a hyper parameter.If you change that, you would be generating another trajectory.Change that again the temperature, you will be getting another trajectory.So from this older policy, you generate a set of such trajectories and then for each of them you calculate the reward and advantage for Allstate action pair.So we have all the state action pair like AIT, S IT.So I is the particular trajectory and T is the time point, right?So for all each of this pair reward value and what is the advantage value, right?Somehow we know we will talk about this.How do we know that, right?And if we know that we can compute this now from this, we will be now what?What it have done, it has done, it has collected some trajectories and the next step, we create mini batches.So for example, this can be a mini batch, this can be a mini batch, this can be a mini batch, right?So take a mini batch and run a gradient this ascent.So you take a mini batch of trajectories, run a gradient ascent.What does it do?It updates the parameter of the online model, right?So remember we talked about two models, one is the older model, other is the model which is being updated right now.You run K number of epochs here and then after we have done with all the epochs, we will be going for the next iteration of training.Now what we can do, we know that we have updated our model.Now we can replace the older model with this new model because our model has already been updated.I have a better way of regenerating trajectories.So what we will be doing, we will be copying the model that we have just learnt in this iteration to the older model and from this older model we again sample some trajectories and we do this for multiple iteration.We do this for multiple iterations.Now one thing that you may consider that we are maintaining 2 models.So that is not really true.We can optimize here.So we have at least two models like Ï Theta old and Ï Theta.So basically what we are doing, we are not really maintaining 2 copies of the policy, right?2 copies of the LL.So we first using the older 1 and sampling some of the trajectories, right?And then we can delete the older model because the older model, what is the necessity of that?It just creates or generates some trajectories, nothing else.Right now we update the parameter of the online model which is Ï Theta and then we can copy that to be our older model and sample some trajectories then forget that older model.So after you have done 1 iteration, you are treating the online model or the updatable model to be your your old model which is kind of generating trajectory and then forgetting that.So this is the proximal policy optimization learning setup.So this was the kind of de facto learning algorithm back in say while ChatGPT was trying to align their model with human behaviour.Mostly this was the kind of de facto standard.But gradually other algorithms has have replaced this idea and we will be telling why.Now let us understand what are the models that we need to maintain in this PPO training loop and counting that would be very easy.So we talked about this training loss.So in training this training loss, what is the model that we need to maintain?We will look at this later, the second formula.So this model which is the online model we need to keep track of, right?So though we talked about this Pi, Theta, Volt, this is not required because we are kind of copying that after every iteration and generating trajectory and forgetting that.So though this is a model we are using, but this is this is not required to be trained.Now try to expand this advantage function.What was the advantage function?It was the Q function minus the value function.Right now the Q function we have already talked about it is a combination of the reward and the value.Now given this, we need to come up with some model which so this policy network for this one, I need a policy network.So what it will take us input, it will take us input a prompt and generate the next token.OK, so that we require and in order to compute the advantage function, we need to model this is not it.We need to model this, right?So we need to and also we need to maintain this the reference model in order to compute the difference between the reference model and the new model that I am fine tuning, right?So this is this was what this was supervised fine-tuned model that we have already done say for example with your customer care relationship data that you also need to maintain.So this you have to store, you need to store this model meant this reward model, what this reward model will look like, how will it look like?So given a sequence you need to output what is the reward value right?So may be either positive, negative or a particular value.So how good or bad a generated sequence is right?The fourth model, so it will return a score.The 4th model is the value model which we also need to maintain.What is the input?The input is the current state.
02:15:12 - 02:29:07
DPO Implementation and Evaluation
The output is the the value of the next state.What is the next state adding a new token.So basically for a new generated token, what is the worth of that?So this is it is computing.So now we have to have 4 model with US1 is the policy, network, reference model, reward model and value model.So a train a PPO model, you need to have all these four model maintaining with you, right?So let us look at how the policy model will look like, what a policy model is, it is nothing but an LL, the LLM which you are fine-tuned, right?So again, it will be acting as an LLM does, right?So the input sequence, you will be passing that through a decoder layer with say causal masking as we do in a decoder.So you will be having hidden state representation, you will be computing logits and then we will be computing softmax to generate probability distribution.And from each probability distribution at every place you will be generating the tokens.So this is normal how LLM decoder auto regressive decoder does.So this is going to be a policy model and finally this is going to be your output.Now the reference model which is a kind of supervised fine-tuned model, the LLM model which you have fine-tuned with your customer relationship data.So that you have already done and it will be following similar pipeline as we have trained the policy model.Now, reward model is something that is new here, right?So what does a reward model look like?How do we really go about with that?So it is very difficult to come up with a reward model.Why is that?To get a reward model, what you want to do?So you want to give us input and input sequence and to score that sequence, right?If the sequence is very good, then you score high.If the sequence is very bad, then you score low.So for example, let us say you want to make it harmless, right?So if your sequence is very harmless in nature, then you score high, otherwise score low, right?But that that is very difficult to get.So for example, let us take some prompts.Let us say I have a question, where is Shanghai?Now the answer is say Shanghai CT is ACT in China.So if I tell you give me a kind of reword for this from zero to 1.0, it should be very subjective, right?And the score can vary, right?Now if I have a prompt like explain gravity like I am 5, then this response, I do not know how much it is good or bad, right?I can say it is good or bad, but how much it is good or bad, that is sometimes very difficult to say, right?But human, they are very good at comparison, right?So that we can compare very well.So now what we will be doing, we can generate multiple answers like say very Shanghai.I generate with the same LLM with some tuning of the parameters to get say 2 responses.And I ask the human, ask the human being tell me which one is better.Rank them for for example, they can tell sometimes very easily.So for example, this case 1 is better.For the second case, say 2 is better.For the third case, say 1 is better.So this is what we call as preference data and that is the kind of data by which we will be training our reward model.So ideally what we want, we would be giving us input a sequence and again our transformer architecture another LLM and output would be a reward value, right?But what we have with us, we have with us a format like X and two outputs Y1 and Y21 being better than the other.So this is the format, right?I do not have any mechanism of scoring right.Did we have that or had we have that with us, we had would have learnt the regression model so that we cannot do so for that.Bradley is someone who can help us with this.So again, the idea is that I would be fitting in a response to the reward model and the reward model would output a reward score and that we will do with a loss function which we call as the Bradley Terry loss.So let us look at the inner part of it.So what is our input?Our input is X, the input sequence or prompt.And we have two outputs, 1 is WYW, other is YL.So this is the winning one and this is the losing 1 S That means WY West has been chosen by the human better than YL.So this is the chosen.This is the rejected the human.When was presented with the with this preference labeling, the human annotator told that this I choose, this I reject.Now you can see the difference is a loss if I assign a score to YL and I assign a score to Y West and YL and if I have trained the model correctly then this difference should be positive.RX y -, r XYL should be positive.Now to make it differentiable or some other for other reason, we are using a sigmoid function on top of that and a log probability.So it is a log value.Now as you have done other modules, so let us look at the sigmoid function.So sigmoid function looks like.Let us try to look at this loss function.This is very important.So at zero we have point value 0.5 and while we are moving towards positive direction from zero so the value is increasing and saturating to 1 and as we are moving away from 0 towards the negative direction the value decreases and going towards 0.So that is the format the form of the sigmoid function.Now if you take a log of that the log of a Sigma this sigmoid function or log of a value look like this log function looks like this.So now this sigmoid function is limited by the value 0 to one this range.Now let us take 2 scenarios.1 is RXYW is greater than RXYL though this is a good scenario.Your model has been trained properly.You want this or whatever score you are assigning for this pair XY W if it is greater than the score xyl that is better.So if it is greater than if the sigmoid then the sigmoid will be returning greater than a value of 0.5 now so this belong to this region greater than 0.5.This part Now if it is greater than 0.5 so for this region as you can see the loss value again in the negative direction but not that great, right?So the loss will be a small number, right?But if this is less than the loss will be a large number.So what this Bradley Terry loss is modelling very elegantly that whenever you have a winning response then you are assigning high reward.If this is the losing response, then you are assigning a low reward and you will be using this right now.This is the workflow to train a reward model.So you have a preference data in the form of XYL, WL.Now you are segregating the prompt, the chosen response, the rejected response, never concatenating this X with YWX with YL.And you have the same reward model, right?And reward model is nothing but an LLM which has been pretended or supervised fine tuning.On top of that, you have a linear head which outputs a well that is important and it is parameterized by this Theta, right?Now we are feeding these two and while I am feeding this XY West, this is generating a score of R Theta XY West, right?And when feeding this xyl it is generating this R Theta XY.And I know I have everything to compute the Bradley Terry loss.So I feel sorry there will be a connection from this also.So these two will help me to compute the Bradley Terry loss and we can use these to back propagate and update these parameters, right?So this idea is very simple.So we are separating these responses, one being chosen, rejected, creating 2 examples, feeding, feeding them to the reward for each example.We are computing the score with the forward passes, 2 forward passes and the scores are helping me to compute the Bradley Terry loss.And using the Bradley Terry loss, we are updating the parameter of this R Theta by optimizing this loss right now, how to implement this right?So now what we have, we have got this prompt plus YLY West and prompt plus YL.And we are creating a batch out of these two, right?And we are feeding that batch to the reward model.And we create a score variable.Now the score variable has got 2.So this is a kind of array which has got 2 entries, the score, the first one is the score for winning response and the second one is the score for the losing response.And you know you can use this to compute the Bradley Terry loss and you can use that loss to do the back propagation and updating the gradient, updating the parameters of the reward model.So that is how you can implement a reward model.Now we are pending with designing the value function network again.It is similar to what we did a large language model.Language model only difference is that at every token generation state, we are changing a state.Whenever you are generating A token, that means we are having a changing state.Whenever you are having a changing state, we need to compute the value of that, right?So it is a regression problem.We are generating a value.So this regression should have a target, right?Our target is that the utility of that state.How do we compute the utility?So basically the reward to go, if I add that state, what is the reward that I am going to get, right?So that is why we are using this reward to go to be the target to learn this value function, right.So now the overall model architecture looks like this.You have the same supervised fine-tuned model.You are using the head projecting that to compute, to make use of to to realize the policy network in one place.And in another term, you are realizing the reference network.You can use the similar thing to compute the Oregon, get the reward model right and of course the value function right.And this is the kind of workflow for Instruct GPT, which follows this training paradigm using PPO.So the first step is you have your domain, you have some prompts which are collecting from the data set, like explain the moon landing to a six year old, right?And a labeler or human generator or a human annotator who can write a response, good response, right?Some people went to the moon, so and so the writer may be right.And then you train a large language model or supervised fine tune a large language model using this data, right?
02:29:08 - 02:44:17
DPO Demonstration and Training Curves
So this becomes your reference model, right?Now what we do in the next phase collect comparison or preference data.So this is where you are collecting preference data and using that preference data, you are trading the reward model, right?So now what you have, you have again the prompts collected or taken out from the data set, right?Now you can tune your already trained model, which is the reference model to generate multiple responses right now, you ask a labeler to rank them.That is where the preference data is generated right now.We have already seen how with a preference data we can train a a reward model.Right now, as we have that, then we can now train the policy model, right, using the PPO algorithm, right?So where we have everything, we have got the policy.This is the policy network, this is the reference network.And we can maintain a value function network here.And we are using the reward model to perform the PPO, right?So this is more or less in a nutshell how the insert with GPT works.So this ChatGPT version is nothing but instruction fine tuning where you fine tune your model with instruction data set and performing a reinforcement learning human feedback on dialogue data, right.So this is a kind of excerpt from the blog which you can look at at your laser.So mostly they are saying that we have used RLHF using dialogue data.Now the last part which would be very easy to understand and much more effective than RLH, the direct reference optimization or DPO.So why DPO came into picture?So in PPO we did this, we were playing with four models, right?So basically we had fit a reward model with human preference data.Then we used the reinforcement learning to optimize the policy network using this reward model.And we also used something called reward hacking or KL divergent penalty to tackle this.Reward hacking thing were good but it is a kind of indirect method at and it involves maintaining 4 models.And think about it you your large language models are say 30 billion parameter, 40 billion parameter, 100 billion parameters right?And you are maintaining four such models right?Secondly, reinforcement learning in general is is very difficult to train because in many cases you will be seeing instability even if you are changing certain hyper parameters varying in a very small way, right?So they are very sensitive to hyper parameters.Right?Now question is can we think of a direct method to perform this alignment tuning?So that is the question that is being addressed by DPO.So the crux of the idea is that, so this was the kind of approach in PPO, you have a preference data, you train a reward model and use the reward model to train a policy model, right?And that you did with a kind of real workflow loop, right?Now what we are proposing here, we want to do away with this part.We do not want to have an explicit reward model.So I am not saying that I am removing the reward model, but I do not want to chain a reward model explicitly, right?Because I need to use reward, right?So basically the idea of DPO tells that if you have a preference data directly train a policy model without taking help of this RL workflow.So basically we are talking about RL free training workflow.So for that we define a loss which is somehow resembling with the loss we have just seen earlier while we train the reward model.So that was something like this Sigma RXY W -, r XYL and there were logs and other things, right?Just focus this part, right?So you can see that this part is being replaced by this ratio and this part is being replaced by this ratio, right?So what does it tell to us?This is telling me the probability of generating A chosen response YL given the prompt X and we are kind of judging that we in the light of a reference model.So this is kind of pulling you closer to the keeping you closer to the reference model, which was kind of modelling that KL divergent one, right?And here in this part, we are kind of measuring how much the the winning or the losing output YL is doing with respect to the reference model right now, if you have a good model, then you want this one to be greater than this one.Probability of generating the right response WL should be greater than the probability of generating the losing response.So basically this is modelling in more simpler term, what is the probability of WL greater than WYW, greater than YL given X and we have to maximize this probability.So this formula is is modelling this idea.So 2 takeaways from this slide or this formula.Again the formula looks scary but do not look at the phase value of this.This is very simple.We are trying to make the probability of generating correct response greater than the probability of generating wrong response.And we are doing that in in connection with the reference model because we want our responses to be closer to the reference model.And you see, the thing has become very, very simple.So we did lots of thing, some of the things were going out of people's head and the kind of training loop was very complex.But here entire thing has been captured with a very simple formula and that takes us to the this DPO workflow.What we do we collect a set of diverse prompt and for each of these prompts using the reference model.We will see how this reference model can be derived.We are generating 2 outputs Y1 and Y2.Now we want one.We will be engaging human annotator to tell us which one is better than what right?So the human annotator will say out of this Y1 and Y2 which is the chosen and which is the rejected.So basically from this I am generating the preference data.OK, Now I know if I have the preference data, I can use the loss function which I have described in the last slide to train our model.As simple as that.There is no reinforcement learning, there is no maintenance of these four models, very simple.Now we need to talk about this to close the entire training loop.So what we have, we have this preference data, right?The preference data looks like this, a prompt and a chosen response, a rejected response.So your data will be looking like this.Now you can take the the prompt and the chosen response to train reference model by doing supervised fine tuning and use that to the DPO training which we have already discussed in the last slide.OK, so with this, I think I am done for today.Again, just to reiterate, this is not really a very easy concept, but this is one of the most important ideas in today's large language model foundation model world.Every large language model has to go through this workflow, right?PPO is a very hard algorithm to understand as well as very hard algorithm to train.But PPO is very simple both in conceptual notion and also in in implementation wise.So these are the two resources which are super useful and super simple.So which I request you to tally the slides with these two resources which really explains the idea with associated code segments.So with this, I will stop now.I think we have a mentimeter session, so let us go to that.OK, I think the first question is visible.The question is what is reward hacking in the context of RLH?The first option is the user manipulating the model through prompt injection.The policy forcing the reward model to get high scores without actually producing good outputs.A security vulnerability in the training infrastructure.A reward model refusing to score certain responses.So let to let us get to the answer.I think majority you have of you have given the correct answer.The correct answer is the policy forcing the reward model to get high scores without actually producing good outputs.The next question what is the key advantage of DPO over standard PPO based RLHF SO which we have just discussed.The first option is DPO eliminates the need for a separate reward model and RL loop.The second option DPO achieves a higher raw reward scores.The third option DPO requires less human label data.Fourth option, DPO only works for very small models.OK, so let's see the correct answer.OK, so majority you have of you have given the correct answer.The correct answer is DPO eliminates the need for a separate reward model and real loop.The real model is implicit.It's not that there is no reward model.Only thing that we should consider the reward model is implicit and there is no RL loop.The last question in Mente is how many distinct models are typically required in a standard PPO based real HF pipeline.First option one, just the policy.Second option 2 policy and reward model.Third option three, policy, reward and reference model.4 Policy reference, reward and value model.The correct answer is 4 because it maintains policy model, reference model, reward model and the value model.So those who have selected the third option so they have left out the value model which is also is maintained by the PPO based.So with this, I will hand over to Rekha for the Hanson session.So Hanson session is framed around the demonstration of the direct preference optimization, which is again a very simpler algorithm to understand as well as code.
02:44:18 - 03:03:03
DPO Hands-On Session and Sample Analysis
So over to Rekha.Thank you Sir.Am I audible to her?OK, someone is saying too fable.Is it OK now?Yes, Rekha, it's good now.OK.Thank you Manish for confirmation.Is my screen visible to all?OK, so good afternoon to all.In this demo we are going to discuss the demonstration of the DPO.In this regards, you will understand what is this DPO is and the data formats for the DPO and the actual implementation.So we have some steps.So along that steps we are going to complete the demonstration.As usual we start with the some packages that we need and the settings is for the reproducibility.So this is the very first block.Next, in this particular case as you already know that in BPO we have a SFT model means a preference trained model.So that model we are taking is a clan model and you can see the instruct.So it self suggests the instructor would suggest that this is trained on a instruction data set the instruction with the instruction tuning which is one of the type of the SFT.The remaining parameters are as follows.For the you can say this demo.Here you can see the beta that you have seen for the KL divergent.So the beta value so we are setting is as 0.1.There is an issue with the can you check?Can you get closer to you the mic?So these are the you can say the hyper parameters we use during the training the DPO implementation.So before lending to the DPO implementation, let's get the intuition of the DPO.So in that particular one, the only thing is the loss, the DPO loss.So that is the primary part of this implementation.So in this particular case, you will see the hand coded example for the computing the DPO loss.So in this particular case we now the four things 1 is a you can say the values with the you can say policy model for the chosen and the values for the rejected one from the policy model and from the the same thing from the reference model.The model which is not you can say for the train with this you can say setup so the reference model.So here is a sample function or hard coded function and the is DPO loss scalar taking all the four values along with the beta value which is you can say one sort of the things that controls in between these two.So first we are computing the reward for the chosen ones taking the values of both the chosen from the policy and the chosen from the reference one.Similarly we are computing from the for the rejected 1 using the you can say policy rejected and reference rejected.Now using both values the reward values we are computing the margin.So margin is here like reward of chosen minus reward of rejected.So in that particular case you can see once the margin is computed then we need to do the you can say the computation, the loss computation.So in that particular 1 the sigmoid and then loss.So this is the return.So here you can see at the very first or you can say beginning both models, you can say given both models are the same means without training the responses, we are assuming that the responses from that is the same.So in that particular case you can say the difference will be the zero.So the loss is going to be equals to the lane 2 or you can say log of two.In the other cases like let's say the chosen is preferred with the policy model, in that particular case the loss is going to be you can say lesser compared to the, you can say the baseline in the second one and in case of the rejected one, the value is going to be higher.So this is the just intuition of the DPO loss with one hand coded example.Now in the actual, it is not this you can say implementation in actual we need to do with the huge you can say with the data sets like the data set have the you can say question and the corresponding responses.And the responses are two types of 1 is a chosen response, second one is a rejected response.So that sort of the things is you can say main.So let's do the actual data preparation using one standard data set.So for that particular one, what we are doing first is first we are taking the you can say tokenizer.So with the help of this tokenizer, we are going to encode our sequences from you can say the user prompt or the chosen response or the rejected response.So in that particular case if the tokenizer is not having this, you can say paid token in that particular case that we are putting as a end of token 1.So in this particular case, the actual data set we are taking is this particular one data set, this ultra feedback binarized data set.So it is you can say some sort of the questions and for that there are some sort of the responses.And in the responses you have the you can say values like this is the question and for that this response, the chosen response and the rejected response.So let's see some sample from this particular 1.So as you can see I am printing the first sample of this data set.So in this particular data set the data you can see samples presented in the form of the dictionary.So from that particular one first we are taking the keys and using that particular keys we are taking the which what is the chosen one, what is the rejected 1 and for these two we have the score as you have seen in the you can see slides.So you have seen that there were two response Y1 and Y2 and in some cases first one is record and in some cases second one is referred.So that sort of score is a score chosen and a score rejected.So here you can see this is the data set.It's having the 2 split the data set as a train and the test and we are taking the first sample of this train 1.So that has four values means you can understand that 4 columns.So like this, so we are printing the first one means the chosen one, then then we are printing the rejected one and the scores.So you can see here 6 values.So 6 value indicate that the chosen response is given to 10 humans and among that 10 humans 6 selected that particular one.Similarly the rejected 1 so given to the 10 humans or 4 selected that particular 1.So that score indicates value.Or you can say things from the 10 if you look get the data set on hugging phase.So this is the exit data set present on the hugging phase.Now it is not the format that we need for this DPO implementation.So we need to prepare as per our requirements.For the preparing as per our requirement there are some functions so that will be you can say helper function to prepare the data set.So the very first function is what are 2888?So this particular function take the examples and from that particular one, first we get the chooser from that chosen one, we are getting the text to accept.The final assistant term simply means that it is a user instruction.Let's say the question, OK, now the next one, the next step we are doing is we are, you can say applying this chat template on this prompt message.So with that particular one, what you have seen is it separates the boundary.So it's a start with you can see I am start, then you can see some assistant message, then the end, then the start of user and end of the you can see user and at the end you can see end generation prompt is equals to true.So you will see the assistant.So it simply means that our assistant is enabled.We are looking for the responses.So in that particular format we converted our prompted text.So chosen is as it is the sequence and the rejected is like this.Now taking these three we are preparing the triplet.So in the triplet prompt first one second one is a chosen 1 and the third one is a rejected 1.So here you can see I printed this.This is the user.So here you can see I'm start and you can see the system role, the default one because we use the chat template and then this is the user 1.So this is the user instruction or the user query.And this is the end of user query with the I am end.And then you can see the start assistant as we enable the prompted generation is equals to true.Now both the responses, the very first one is the chosen one and the next one is the rejected 1.So with these particular functions, the triplets are prepared.Now as we know that if this particular data set has the two set, 1 is a train set and the second one is a valid test set.But when we are doing this kind of training, what we need is we need in the three splits.So this particular part is for the splitting or you can say this designing 3 splits.So the very first one is A1 function we are defining this is simply a function which you can say split our data set based on what is the start value and how many we want in a split.So first we are taking we are creating for the train.So we are calling this and we are passing it start from the zero and goes till the you can see the train sample we have defined earlier.The validation is again from the you can say train set.So it does start from the you can say end of train and goes to the you can say up to the well size.And next one is it's from the test 1.So it's directly start from the zero and goes to the default we have defined let's say the 100.Otherwise it goes to the end means -1.Now we are printing the sample sizes using the length.And then we are again printing these you can say 3 values from chosen and rejected of the first one means the tree sample.So you can see these are the values.So we are taking 500 for the training, 100 for the validation and 100 for the test.So with that particular one, we got the triplets and you can see we have the splits.Now we need to convert that into particular data set that is supported by the hugging face models.So here first we are defining a you can say the standard class you can say inheriting the data set.So first one is we are you can say storing the triples of you can say type train test and the validation.And then the length means total number of rows in that particular one.And the last one is you can say getting the one item Now next is tokenizing the page.So we have is 3 things 1 is a prompt, next is you can say chosen and the next one is a rejected 1.So based on that particular one, what we are doing is we are preparing the prompt, we are preparing the prompt.So the prompt is like prompt then chosen prompt then rejected.So for that this is a function.And in that particular case we are passing the prompt and we are passing the completion.1st we are applying the tokenizer and we are getting the IDs, the token IDs and then similarly we are passing the completion and we are getting the IDs.And what we are doing is we are we are keeping till the Max length and we are you can say defining a mask based on the you can say length of the IDs of a prompt and the you can say the completion one and then it returns the two things one is IDs and the mask that will be you can say for the data loader preparation.So as I said that with this particular one, what we get is we get the IDs as well as we get the mask.So you can simply see the IDs for the first or you can say for the you can say 1 sample is like this and we have taken the Max length is you can say 128.So you are getting that 128.Now we have this data set and we have this.You can say different three different splits but in that particular one the values as not as the final you can say input requirement of the model.So for that this is a call it function.So this call it function is going to you can say arrange this values per batch.In the call it function we have further function which is you can say the pack.So with that particular one we are packing for the chosen one and the rejected 1.So here first we are simply initializing the sequence and the mask.And for the examples in the batch, you can say we are getting the IDs and mask is simply calling the tokenized pair.And then what that particular one we are appending you can say into the sequence and the mask we are appending in the mask one and now we are getting the Max length and we are defining you can say these three values initially.So initially you can say input IDs of.Type you can say pair token.So initially it's let's say the pair token value is 0.So all the zeros of the you can see this particular length and then the attention is all zeros.Then this completion mask is again all zeros.So this is it.You can see before being filled with the actual values.Now we are iterating over this sequence and the mask that we got.Using this you can say tokenize, you can say pair function.So with that one we are updating the values of ID's attention and the mask one and now we are packing that.With that you can say chosen and rejected one and we are returning this you can say chosen and rejected.So using this particular call it function, we are creating the three data loaders.So simply the train triplets we have defined and the bed size we have initial you can say defined earlier and in case of you can say the train we are enabling it shuffle and based on the collate function.Similarly for the validation and the distal order.So if you see closely the things in this particular loaders, the samples will be like this.There will be first the chosen one IDs, attention mask and the completion mask, rejected input IDs, attention mask and the completion mask.So here you can see getting the values for the first batch.So here as I said that for the chosen 1, so this is you can say IDs as I defined the batch size is 32 so it's 32.And again this is the attention mask of the chosen, completion mask of the chosen.Now followed by the you can say rejected 1.So with that particular one data set is prepared and we got the things as we required for the model.Let's download the model.So for that what we are doing is we are defining 2 models.1 is a policy model so that we are loading from the pre train by passing the model ID.As I said earlier, it's SFT means instruction tuned model and the data or the type we are using for this particular 1 is a float 32 and we are passing this model to the device.Similarly, we are loading the reference model and we are putting this reference model first in evaluation state and whatever the parameters of this particular model that we are disabling, it simply means that we are freezing the reference model.We are not going to train this model, only the training happens for the policy model.
03:03:03 - 03:14:52
Q&A on DPO and Real-Time Application
So here we are defining the optimizer and in that particular case you can see that this is with the policy parameters only and with the learning that we have defined earlier.So with this both models you can say loaded.So the illustration of DPL loss is as follows.We have a prompt, the blue colored one and we have the you can say chosen one which is you can say green colored one and the rejected 1 is you can say red colored 1.So here you can see some samples.So we are passing these two.Model 1 is you can say the reference model at the bottom and we are getting the values, the log probabilities and similarly we are passing the policy models and we are getting the log probabilities values.Based on that log probability values, the DPO loss computation is happening.So using this reference values and using this policy values, the loss computation is as you can say depicted.The actual loss is like this and what these values are in it you can see here.So in this particular picture, the main thing is what sequence log probability we need to know what is it and how we can compute it.So for that, here is an example.So it's a simple numerical example.Let's say we have a work cab of size 5 and in that particular work cab we have the IDs.You can say 012344.So whatever the you can say sequence we have as a sample, there are they must have you can say IDs from this one only, from zero to four only.So let's say one input ID is this particular 10123 and for that completion mask is like this 10011.So it simply means that 11 mark that the response tokens.So at the very first one we compute the logics to get the log probabilities.So we are passing this thing to the model and the model gives the logic.So in this particular case, you can see, you can see we have the first one, so zero to 1, one to two and two to three.So three logic values you will see in this particular case and the shape of that particular 1 is like this.So the depiction is here you can see more illustrative.Once we got the logics, what we need to do is we need to shift input IDs as well as the mask.So we are shifting like this.Next we need to convert into log probabilities.So for that we are using this log softmax on logit.So with that particular one we will get the log probabilities.Now from this particular one we will gather the probabilities for the actual target tokens.So let's say you can say the label is 123.So in that particular case you can see only last two were the part of the token 1, so only last two.So how we are getting.So if you see this is for the zeroth one, this is for the you can say 2 and this is for the you can say that last one.Based on that particular one, we got this token log values using that particular one along with the mask we will get the final one.So we are multiplying with this one.We are getting these values.So the sequence log probabilities is what the summation of the probabilities of the target tokens.So here what we are doing is we are doing the summation.So this is the thing that we need in that particular function.So this is a simple demonstration.The same thing in this particular function.So the name is the sequence log probabilities.It has four parameters model input IDs, attention mask and the completion mask.And using that particular one first we are getting the logics, then we are shifting the you can say IDs and the mask.Then we are getting the log probabilities.Then we are multiplying, we are gathering the you can say probabilities for the target tokens.And then what we are doing is we are summing for the target only.So this is the sequence log probability function.Using this particular one, we are going to compute the DPO loss.As you can see in the image using the sequence probabilities, this DPO loss is computed.So that particular part is happening with this function.So DPO step.So in that particular case, the chosen one and the rejected 1 is given and that first you can say from that one we are getting this ID's attention mask and the mask.Similarly for the you can say rejected one.Now we compute first with the you can for the policy 1.So in that particular 1, the gradient flow is going to be gradient flow will be enabled.So simply you can say we we call the function the sequence probability function.We pass the policy model along with the input values.So these are the you can say 3 input of a chosen 1.Similarly, we calculate this sequence probabilities with the policy model of You can say rejected 1.So we got the you can say values for the chosen 1 and for the rejected 1.Similarly we compute for the reference model.In that reference model training is not you can say happening that is just in a you can say frozen mode.So what we are doing is with the no gradient we are getting the values like the reference chosen and the reference rejected.So based on these four values we are computing the rewards, the chosen reward and the rejected rewards and using that rewards we are computing the margin and then the loss value.So this function at the end returns three things 1 is a loss and reward mean for the chosen 1 and reward a rejected mean for the rejected 1.So here you can say like this evaluation loss but you can say loader.So first we are putting this model in the evaluation state then we are you can say defining 2 values total NN.So initially total is 0.0 and N value is 0.And for this chosen and rejected in the loader, we are computing the values, you can say the chosen and the rejected and then we are computing the loss.So that particular one is loss evaluation function.So with that particular one sequence probability function is defined.DBO step is defined to compute the final loss.Let's move to the training pipeline.So we are doing training with the early stopping.So first we are importing the copy to keep the copy of the you can say model.So we are using patient two.It simply means that if two consecutive epochs with no improvement then we stop.And initially this you can say history all the empty one.Now you can say these values initially all these the empty one or you can say zeros.Now for the epoch in the range of 1/2 epoch plus one, we are putting this policy model in a trainer state and the running loss is equals to 0.0 means the plot 1.So we are taking this chosen and rejected from the loaders and 1st what we are doing is we are passing to the devices.So in case of this chosen and rejected, what we are having is we are having three parts.One part is the ID, second part is mark attention mask, third part is what the you can say mask completion mask.So for that we are using this T variable.Now using this chosen and rejected we are computing this you can say values the laws are chosen and are rejected by calling the function that we have defined earlier.Now this is you can say things for the back propagation and we are you can say keeping this running loss and we are appending the values whatever we are getting in the you can say history that we have defined earlier.So that way you can say that raining function.Now these are the training curves.So in that particular one with that particular one, we are plotting 2 things.One is the loss.The second one is you can say the rewards.So if you see these values like this, you can say losses over the E box as we you can say implement.So this is you can see.So based on these values, you can see that the policy model is learning.And also in the right hand side, you can see the implicit loss is as well as.So both are going apart.So the yellow one is the rejected one and the blue one is the you can say choose one.Now let's evaluate on the test set.So we are getting the test evaluation values.So here you can see on the test set the loss value is 0.6 set and the margin is you can say in the plus.So you can say there is a little improvement.Let's see you can see the performance on some samples.So we can take some samples and for that we can see the values like the chosen 1 rejected 1 and based on that what is the margin values.So for that, this is a compare model function.So simply take that sample ID that is from the test set.And for that we evaluate both models, the policy and reference and compute the values.Now let's see in the test set we have, you can say define hundred samples.So from 100 samples we are taking the sample #1344 and 52.And for that, let's see what is the values.So here you can see that using the word we are getting is plus 0.216, rejected is like minus 0.792 and the margin value is this particular 1.So you can clearly see that the policy refers the chosen response.Similarly, this is you can say sample 44 results, sample 52 results and let's see for some other samples.So you can do for the other samples.So in that particular 1, you will see that model also preparing some rejected reference in some cases.So this is a simple demonstration for the DBO.Thank you.OK, thank you.I think we can now move on to the final stage, which is the QA.
03:15:10 - 03:29:05
This is the question by Mr.Thoron Burma.Human feedbacks can be biased or user can try to manipulate LLM output by giving wrong feedback.How is this address?So this is a real concern.So whenever we do some annotation on any any machine learning model for it is not only relevant to this feedback data for alignment tuning.So it is not like that we take input from only one annotator.We take input from multiple annotators and we always try to see whether there is an agreement between the annotators.Is it fine now?Is it OK now?Yes Sir.OK so again the question I repeat, human feedback can be biased or user can try to manipulate LLM output by giving wrong feedback.How is it addressed?So as whenever we create a data set for any machine learning algorithm, so whenever we are getting it annotated by a single person.So there is always a kind of bias.The bias can be implicit, bias can be explicit.So in that case, if we take the input only from one annotator, that risk is always there.But what you can do or generally people do get the same data annotated by multiple persons and check whether there is an agreement and based on those agreement we come up with a agreed data set which is generally used in training the model.So here the notion is if we are taking view from multiple people.So mostly if we are talking about a general thing or even a domain related thing, many people will say the similar thing if that is correct.So that is how we handle bias or manipulation.The second question, what is the significance of the overlapping area here in the KL diversion?So that is the area where the two probability distributions match.So if two probability distributions are exactly the same, then the overlapping area would be the area of either of these probability distributions on the distance will be 0.OK, I think we are done with the typed in questions and that's open to all.If you have any questions I'll be happy to answer.We would request if anyone have any query please raise your hand.I will unmute accordingly.You have access to Mike?No, I don't have any questions.I'm good.OK.Arav Santosh Kumar.Hello, Minutes.Thanks.Thanks.Hello, Sir.Good day.Am I audible, Sir?Yes.Yes, yes.Good days.Yeah.So regarding that Director preference optimization, do you have any special techniques where LLM operations are integrated for our DPO process?OK, that's some of the processes on the diagrammatically you have discussed it clearly.So are there any LLM operations are predefined over those DPO processing integrated to the DPO process or if they are integrated, So what are the separate DPO processes specific to the optimization and comes under both LLM operations?OK, so the starting point is a reference model, OK, OK.Now you have the preference data.Now the way the preference data is created is you have already a safety model and you're giving a prompt, you're generating multiple responses and then you're getting that those responses to the human annotator and they're right.OK Out of that you create the preference data.Right now you use the preference data to train the baseline is SFT model.You are kind of updating that, right?OK.Now basically you're keeping 2 copies.The reference model other is the model that you are updating, OK.So the updating model is like a prompt model, right, which we are doing it integrated to the prioritized optimization, right, based on reference model that is what right.Yeah, right.OK, OK, OK Sir, continue please, Sir.Yeah, I have been interrupted you please continue.Yeah.So basically you are updating this model keeping that closer to the reference model, right?OK, OK.And the way we should be updating that the probability of the winning response should be greater than the probability of the losing response, OK.So do we need to calculate the probability separately over here or the model will automatically give a result of the probability?So model will take care of that.So define the loss function, OK, it will so the basic mathematical functions like which we inbuilded under Excel.So those mathematical functions which would not required to be redefined again under the model, it will come up automatically with the loss function.That is what the system begins, right, I believe, Yeah.OK Sir.Thanks.Yeah, yes Sir.What Rica has presented it is a detailed implementation of DPO, but you can do it far more easily by calling the DPO function.So there is a DPO library you can just call the function and train the model.Yeah, yeah.Directly it will take the LLM function of our model and it will call the original, what we say the DPO function for that reference model.That is what the base.Yeah.Yeah.OK, OK.Yeah, yeah.Thank you very much.Thanks.I can, I am clearly understood.Thank you.Siddesh, you have access to the mic.Yeah, Mr.Siddesh, I think you can speak.Yeah.Thank you Sir.Sir, actually I just wanted to ask you one the this TPO in the real time.Currently I am working as a as agent Ki associate director.First I am looking for to build any use cases related to.Currently they have going couple of things.Just I wanted to understand where we can able to implement this one in the real time and how to prepare the data from the scratch.Can you able to just guide me?OK, sure.So I mean I don't know what is a domain of your pharma domain, Sir, pharma domain.So you have to have a baseline model which should encode the knowledge of pharma.OK.So how you can do that?You can have data which is collected from the pharma domain and pretend a model, right?And you can further supervise fine tune that by creating a fine tuning data where you can say for a given question, for formal related question, what can be a correct answer, right?Yeah.In that data, you can fine tune an already pretend model and that gives you this reference model.So this part is clear, right?Yeah.Now let's say you want your model to be polite.Let's assume so you want to have an assistant which is polite.Now for now, you can generate certain prompts, write some questions which you have already and using that you can ask the already fine-tuned model to generate a set of responses, right?The way to do that there is something called temperature.So you can change the temperature, the value of the temperature and as you change the value of the temperature, it will be generating different responses, correct?Now depending on how much you are changing the temperature.So, if you are changing from the optimal value very much, then it will be generating a bad response and if you are closer to the optimal value, it will be generating more or less good response.Yes, now you generate such multiple responses.Now you give those responses to an human annotator who will tell which one is the best one for example or human annotator rank, right.So with that you can create your preference data.Now as we have the preference data, you have three things for a given from what is the winning response?What is the losing response, right?And you just feed that to the DPU model to optimize that loss, right?So you have everything, only thing that you have to be concerned about how to supervise fine tune your model and how to collect this preference data.These are the two things that you need to do to tune the models to your requirement.OK, fine.So actually I just wanted to ask you one more question.Let's say example, currently we have built already 1 chat bot for the HR for live management and policy management.OK, we were use this models open a model we have.So currently let's say example already it is built and still of course there is some improvement needed.Currently we are working on it.So can we use that same whatever already we have built, right?Can you use the same data as a preference data?Yeah, definitely, definitely.Then in that case, what is because actually when the temperature is low definitely it will bring the data based on the whatever way given the RAG system pipeline, right.It is a down level.The whatever the uploading existing information is there in the RAG, it will bring when it is a temperature 0, it is not going to become a creative answer.It will give the exact answer it will give.So if you go with the DPO model, what is the use and how we can able to in the why we can able to make them the ROI for this one, how we can create OK, so this DP or PPO whatever it is, yeah.What is the kind of objective of these, The objective of these two train a model.These models are they're learning a policy, right?The policy is to generate the correct token.And what is the definition of correctness?The definition of correctness is that they would be kind of targeting the social dimension like helpful etcetera, etcetera, right?Yeah, now you are whenever you are lowering the temperature, you are generating a bad response, right.But this bad response will help the model to generate better aligned output.So these are this you are using to train the model to generate better output.See whenever you are.So it's I'm giving a kind of real life analogy, right.So let's say you are training a kid how to behave well, right?So in that case you will be showing examples of which are good behaviour and we will also use examples which are bad behaviour, yes.Now this lowering of the temperature value is like you are generating bad behaviour to tell the model that you should not follow this.So that's what the I mean the idea of using lowering the temperature or having the temperature changed in different ways.Thank you.And professor, the whatever the answer session we have, we need to follow the same thing in the real time or we can just use the DPO function.Now it is enough.You can use the DPO function.That's enough.Thank you, Professor.Thanks.So we do not have any more queries.In the interest of time, we can sum up the session at this point.OK, thank you.Thank you all for being with me today.It was a difficult topic to discuss, but I hope I have shared the overall idea and of course it needs practice as well as far more detailed study.Thank you all.Thank you so much Sir.Thanks everyone.I'm closing the session for all.Thank you.